{
  "name": "estk-read-models-pg",
  "version": "0.0.3",
  "description": "",
  "private": true,
  "main": "lib/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "../bin/ts-mocha test/**/*.ts",
    "build": "../bin/ts-build",
    "build-and-test": "npm run -s build && npm run -s test",
    "prepublishOnly": "npm run -s build"
  },
  "dependencies": {
    "debug": "^2.6.4",
    "estk-events": "0.0.3",
    "estk-pg": "0.0.3"
  },
  "devDependencies": {
    "estk-events-in-memory": "0.0.3",
    "estk-events-pg": "0.0.3"
  },
  "author": "Dave Foley",
  "license": "MIT"
}
