{
  "name": "estk-read-models",
  "version": "0.0.3",
  "private": true,
  "description": "",
  "main": "lib",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "../bin/ts-mocha test/**/*.ts",
    "build": "../bin/ts-build",
    "build-and-test": "npm run -s build && npm run -s test",
    "prepublishOnly": "npm run -s build"
  },
  "dependencies": {
    "estk-events": "0.0.3"
  },
  "devDependencies": {},
  "author": "Dave Foley",
  "license": "MIT"
}
