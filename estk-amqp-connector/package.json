{
  "name": "estk-amqp-connector",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "npm run -s test:mocha && npm  run -s test:lint",
    "test:mocha": "../node_modules/.bin/mocha --recursive --compilers js:babel-register test",
    "test:lint": "../node_modules/.bin/eslint src/",
    "build": "npm run -s build:clean && npm run -s build:lib",
    "build:clean": "../node_modules/.bin/rimraf lib",
    "build:lib": "../node_modules/.bin/babel -d lib --ignore '**/*.test.js' src",
    "build:flow": "../node_modules/.bin/flow-copy-source -v -i '**/*.test.js' src lib",
    "build-and-test": "npm run -s test:mocha && npm run -s build",
    "prepublishOnly": "npm run -s build"
  },
  "dependencies": {
    "amqplib": "^0.5.2",
    "estk-events": "0.0.1"
  },
  "devDependencies": {},
  "author": "Dave Foley",
  "license": "MIT"
}
