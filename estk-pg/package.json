{
  "name": "estk-pg",
  "version": "0.0.8",
  "description": "",
  "main": "lib",
  "directories": {
    "test": "test"
  },
  "files": [
    "lib/"
  ],
  "scripts": {
    "test": "../bin/ts-mocha test/*.ts",
    "build": "../bin/ts-build",
    "build-and-test": "npm run -s test && npm run -s build",
    "prepublishOnly": "npm run -s build"
  },
  "dependencies": {
    "@types/debug": "^0.0.31",
    "@types/pg": "^8.6.5",
    "debug": "^2.6.4",
    "pg": "^8.7.3",
    "pg-pool": "^3.5.1"
  },
  "author": "Dave Foley",
  "license": "MIT"
}
