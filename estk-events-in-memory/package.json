{
  "name": "estk-events-in-memory",
  "version": "0.0.3",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "../bin/ts-mocha test/*.ts",
    "build": "../bin/ts-build",
    "build-and-test": "npm run -s build && npm run -s test:mocha",
    "prepublishOnly": "npm run -s build"
  },
  "dependencies": {
    "debug": "^2.6.4",
    "estk-events": "0.0.3"
  },
  "devDependencies": {},
  "author": "Dave Foley",
  "license": "MIT"
}
